#include "libecs.h"
#include "libecs_private.h"
#include <stdlib.h>

ECS_World *ECS_CreateWorld() {
    // ECS_World *world = malloc(sizeof(ECS_World));
    // world->entity_manager = malloc(sizeof(ECS_EntityManager));
    // world->component_manager = malloc(sizeof(ECS_ComponentManager));
    // world->system_manager = malloc(sizeof(ECS_SystemManager));
    // return world;
    return NULL;
}

void ECS_DestroyWorld(ECS_World *world) {
    //free(world->)
}